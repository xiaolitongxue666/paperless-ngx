---
description: Paperless overlay 用 latest，勿降 3.1.3
alwaysApply: true
---

生产路径 `/home/ubuntu/Code/VPS/paperless-ngx/deploy/vps`。部署：`COMPOSE_IGNORE_ORPHANS=true docker compose up -d --no-build`。

镜像保持 `paperless-ngx:latest`。可用 &lt; 2.5G 不 pull。勿 `volume rm`。OCR 出站 `:17890`。

细节：[README.md](../../README.md)
